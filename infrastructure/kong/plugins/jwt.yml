- name: jwt
  route:
    name: user-route
  config:
    claims_to_verify:
      - exp
- name: jwt
  route:
    name: product-route
  config:
    claims_to_verify:
      - exp
- name: jwt
  route:
    name: order-route
  config:
    claims_to_verify:
      - exp
- name: jwt
  route:
    name: payment-route
  config:
    claims_to_verify:
      - exp
